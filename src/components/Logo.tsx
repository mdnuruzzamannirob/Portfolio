import Link from 'next/link';

const Logo = () => {
  return (
    <Link href="/">
      <span className="font-mono text-xl font-bold">mdnuruzzaman.dev</span>
    </Link>
  );
};

export default Logo;
